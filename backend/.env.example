# Environment Configuration for To-Docx Desktop

# Application Settings
APP_NAME=To-Docx Desktop
APP_VERSION=1.0.0
DEBUG=true

# Server Configuration
HOST=127.0.0.1
PORT=8765

# MinIO Configuration
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=your_access_key
MINIO_SECRET_KEY=your_secret_key
MINIO_BUCKET=to-docx
MINIO_SECURE=false

# Aliyun DashScope Configuration
# Note: DASHSCOPE_API_KEY is now automatically set from the license activation
# The API key is decrypted from the activation code and set as an environment variable
# Leave this empty - it will be automatically configured after activation
DASHSCOPE_API_KEY=
DASHSCOPE_MODEL=paraformer-mtl-v1

# File Configuration
MAX_UPLOAD_SIZE=524288000
OUTPUT_DIR=

# FFmpeg Configuration (optional, auto-detect if not set)
FFMPEG_PATH=
